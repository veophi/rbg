# Default values for rbgs-controller.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

image:
  repository: registry-cn-hangzhou.ack.aliyuncs.com/dev/rbgs-controller
  pullPolicy: IfNotPresent
  tag: d8e43ceb

patioRuntimeImage: registry-cn-hangzhou.ack.aliyuncs.com/dev/patio-runtime:19d69ad8

imagePullSecrets: []

podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 4000m
    memory: 2Gi
  requests:
    cpu: 100m
    memory: 128Mi


nodeSelector: {}

tolerations: []

affinity: {}
